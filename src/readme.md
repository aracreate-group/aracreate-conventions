# SOURCE

The project's own source — application code, firmware, or hardware design files.
The layout depends on the project type (see `src/` by project type in the root
`README.md`). Third-party code you do not maintain is vendored here as git
submodules, not edited in place.
